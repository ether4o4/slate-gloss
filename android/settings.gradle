pluginManagement {
    repositories {
        google()
        mavenCentral()
    }
}

plugins {
    id("com.facebook.react.settings")
}

rootProject.name = 'VistaLauncher'

include ':app'

includeBuild('../node_modules/@react-native/gradle-plugin')
